"use strict";

// cat 400*120

export class Cat {

	constructor(spritePos, x, y, width, height) {
		this.spritePosition = spritePos;
		this.width = width;
		this.height = height;
		this.x = x;
		this.y = y;
	}

}